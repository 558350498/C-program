# References

Use this folder for long external references, teacher-provided constraints, or
copied source notes that are useful but should not be loaded by default.

Keep first-pass docs small. Summarize the durable decision in `../design-docs/`
or `../exec-plans/` when a reference becomes part of the project contract.

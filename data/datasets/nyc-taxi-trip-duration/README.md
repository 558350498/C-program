# NYC Taxi Trip Duration Dataset

Raw Kaggle files for `yasserh/nyc-taxi-trip-duration` should be copied here by:

```powershell
python tools/python_kaggle_download/download_dataset.py
```

Expected local raw data directory:

```text
data/datasets/nyc-taxi-trip-duration/raw/
```

Raw CSV or ZIP files are ignored by Git.
